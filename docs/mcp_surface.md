# MCP surface

_This file is auto-generated by `mix concept.docs.mcp_surface`._
_Do not edit by hand. See `docs/mcp_parity.md` for the architectural principle._

Every public Ash action with a `description` projects to one MCP tool via
`Concept.AutoTools`. Workspace tenancy is enforced by
`ConceptWeb.Plugs.MCPWorkspaceContext`. Actor + tenant filter what each
caller sees.

---

## Concept.Accounts
### Tools (12)

#### `api_key_create`

Issue an API key for the actor; optionally bind it to a single workspace.

- **Resource**: `Concept.Accounts.ApiKey`
- **Action**: `:create` (create)


#### `api_key_destroy`

Revoke an API key by id.

- **Resource**: `Concept.Accounts.ApiKey`
- **Action**: `:destroy` (destroy)


#### `api_key_read`

List the actor's API keys (hashes only; key material is shown only at creation).

- **Resource**: `Concept.Accounts.ApiKey`
- **Action**: `:read` (read)


#### `membership_create`

Add a user to a workspace with a given role.

- **Resource**: `Concept.Accounts.Membership`
- **Action**: `:create` (create)


#### `membership_read`

List the actor's workspace memberships.

- **Resource**: `Concept.Accounts.Membership`
- **Action**: `:read` (read)


#### `membership_update_role`

Change a member's role in the workspace.

- **Resource**: `Concept.Accounts.Membership`
- **Action**: `:update_role` (update)


#### `workspace_by_slug`

Read a workspace by its URL slug.

- **Resource**: `Concept.Accounts.Workspace`
- **Action**: `:by_slug` (read)

**Arguments:**

  - `slug` (`String`, required) — Workspace slug from the URL path.


#### `workspace_for_user`

List workspaces a user is a member of.

- **Resource**: `Concept.Accounts.Workspace`
- **Action**: `:for_user` (read)

**Arguments:**

  - `user_id` (`UUID`, required) — User whose memberships to list.


#### `workspace_my_workspaces_json`

List the actor's workspaces as a JSON array. Used as an MCP resource at concept://me/workspaces.

- **Resource**: `Concept.Accounts.Workspace`
- **Action**: `:my_workspaces_json` (action)


#### `workspace_read`

Read a workspace by id.

- **Resource**: `Concept.Accounts.Workspace`
- **Action**: `:read` (read)


#### `workspace_rename`

Rename a workspace.

- **Resource**: `Concept.Accounts.Workspace`
- **Action**: `:rename` (update)


#### `workspace_set_icon`

Set a workspace's icon emoji.

- **Resource**: `Concept.Accounts.Workspace`
- **Action**: `:set_icon` (update)


### MCP resources (1)

#### `concept://me/workspaces`

Workspaces the actor is a member of.

- **Resource**: `Concept.Accounts.Workspace`
- **Action**: `:my_workspaces_json`
- **MIME**: `application/json`


---
## Concept.Knowledge
### Tools (11)

#### `citation_create`

Record a citation tying a chat message to a source block.

- **Resource**: `Concept.Knowledge.Citation`
- **Action**: `:create` (create)

**Arguments:**

  - `workspace_id` (`UUID`, required) — Workspace the citation belongs to.


#### `citation_for_block`

List citations involving a specific block (forward and backward).

- **Resource**: `Concept.Knowledge.Citation`
- **Action**: `:for_block` (read)

**Arguments:**

  - `block_id` (`UUID`, required) — Block id whose citation participation to load.


#### `citation_for_message`

List citations grounding a specific chat message, ranked.

- **Resource**: `Concept.Knowledge.Citation`
- **Action**: `:for_message` (read)

**Arguments:**

  - `message_id` (`UUID`, required) — Chat message id whose citations to load.


#### `ingestion_job_archive`

Archive a completed or failed ingestion job from the user-visible list.

- **Resource**: `Concept.Knowledge.IngestionJob`
- **Action**: `:archive` (update)


#### `ingestion_job_enqueue`

Enqueue a page for ingestion (chunking + embedding + graph build).

- **Resource**: `Concept.Knowledge.IngestionJob`
- **Action**: `:enqueue` (create)

**Arguments:**

  - `workspace_id` (`UUID`, required) — Workspace the page belongs to.


#### `ingestion_job_read`

List ingestion jobs (queued, running, succeeded, failed) for the workspace.

- **Resource**: `Concept.Knowledge.IngestionJob`
- **Action**: `:read` (read)


#### `ingestion_job_recent_for_workspace`

List the 10 most recent ingestion jobs in the workspace, newest first.

- **Resource**: `Concept.Knowledge.IngestionJob`
- **Action**: `:recent_for_workspace` (read)


#### `link_create`

Assert a typed relationship between two blocks (e.g. supports, contradicts, depends_on).

- **Resource**: `Concept.Knowledge.Link`
- **Action**: `:create` (create)

**Arguments:**

  - `workspace_id` (`UUID`, required) — Workspace both blocks belong to.


#### `link_destroy`

Remove a previously asserted relationship between two blocks.

- **Resource**: `Concept.Knowledge.Link`
- **Action**: `:destroy` (destroy)


#### `tools_answer_question`

Answer a question using workspace content with citations.

- **Resource**: `Concept.Knowledge.Tools`
- **Action**: `:answer_question` (action)

**Arguments:**

  - `question` (`String`, required) — Question to answer using workspace content as context.
  - `workspace_id` (`UUID`, required) — Workspace to draw context from.


#### `tools_search_workspace`

Hybrid vector+graph search over the workspace's pages and blocks.

- **Resource**: `Concept.Knowledge.Tools`
- **Action**: `:search_workspace` (action)

**Arguments:**

  - `query` (`String`, required) — Natural-language query to retrieve relevant blocks for.
  - `workspace_id` (`UUID`, required) — Workspace to search within.
  - `mode` (`Atom`, optional) — Retrieval mode. One of :hybrid (default), :vector, :keyword.
  - `limit` (`Integer`, optional) — Maximum number of results to return.


---
## Concept.Knowledge.Chat
### Tools (4)

#### `conversation_create`

Start a new chat conversation in the workspace.

- **Resource**: `Concept.Knowledge.Chat.Conversation`
- **Action**: `:create` (create)


#### `conversation_my_conversations`

List the actor's chat conversations in the workspace, most recent first.

- **Resource**: `Concept.Knowledge.Chat.Conversation`
- **Action**: `:my_conversations` (read)


#### `message_create`

Send a message in a chat conversation; an AI response is generated asynchronously.

- **Resource**: `Concept.Knowledge.Chat.Message`
- **Action**: `:create` (create)


#### `message_for_conversation`

List messages in a chat conversation, most recent first by default.

- **Resource**: `Concept.Knowledge.Chat.Message`
- **Action**: `:for_conversation` (read)

**Arguments:**

  - `conversation_id` (`UUID`, required) — Conversation whose messages to load.


---
## Concept.Pages
### Tools (24)

#### `block_acquire_lock`

Acquire a collaborative-editing lock on the block. Required before mutating content.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:acquire_lock` (update)

**Arguments:**

  - `user_id` (`UUID`, required) — User acquiring the lock; must match the actor.
  - `ttl_seconds` (`Integer`, optional) — Lock lifetime in seconds before automatic release.


#### `block_ai_answer_evaluate`

Run the AI Answer block's prompt against the workspace and stream cited results into the block.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:evaluate_ai` (update)

**Arguments:**

  - `prompt` (`String`, required) — Question to answer using workspace content as context.
  - `scope` (`Atom`, optional) — Retrieval scope. One of :workspace, :page, :subtree.
  - `profile` (`Atom`, optional) — Knowledge profile. Determines model + retrieval params.


#### `block_archive`

Archive a block (soft-delete; cascades to children).

- **Resource**: `Concept.Pages.Block`
- **Action**: `:archive` (update)


#### `block_create_block`

Create a new block on a page, optionally as a child of another block.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:create_block` (create)

**Arguments:**

  - `workspace_id` (`UUID`, required) — Workspace the block belongs to. Actor must be a member.


#### `block_evaluate_ai`

Evaluate an AI Answer block against the current page or workspace context.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:evaluate_ai` (update)

**Arguments:**

  - `prompt` (`String`, required) — Question to answer using workspace content as context.
  - `scope` (`Atom`, optional) — Retrieval scope. One of :workspace, :page, :subtree.
  - `profile` (`Atom`, optional) — Knowledge profile. Determines model + retrieval params.


#### `block_first_for_page`

Read the first (topmost) non-archived block of a page.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:first_for_page` (read)

**Arguments:**

  - `page_id` (`UUID`, required) — Page whose first block to read.


#### `block_list_for_page`

List all non-archived blocks on a page, in render order.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:list_for_page` (read)

**Arguments:**

  - `page_id` (`UUID`, required) — Page whose blocks to list.


#### `block_refresh_lock`

Refresh an active lock's TTL while the actor continues editing.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:refresh_lock` (update)

**Arguments:**

  - `user_id` (`UUID`, required) — User holding the lock; must match the actor.
  - `ttl_seconds` (`Integer`, optional) — New lock lifetime in seconds.


#### `block_release_lock`

Release a previously acquired editing lock.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:release_lock` (update)


#### `block_reorder`

Reorder a block within its current parent.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:reorder` (update)


#### `block_reparent`

Move a block under a new parent at a given position.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:reparent` (update)


#### `block_update_content`

Update a block's content (lexical state or block-type-specific payload). Writes directly; no separate lock step is required. Refuses only when another editor currently holds an active lock on the block.

- **Resource**: `Concept.Pages.Block`
- **Action**: `:update_content` (update)


#### `block_update_props`

Update a block's props (block-type-specific configuration).

- **Resource**: `Concept.Pages.Block`
- **Action**: `:update_props` (update)


#### `page_archive`

Archive the page

- **Resource**: `Concept.Pages.Page`
- **Action**: `:archive` (update)


#### `page_create_page`

Create a new page in the workspace

- **Resource**: `Concept.Pages.Page`
- **Action**: `:create_page` (create)

**Arguments:**

  - `workspace_id` (`UUID`, required) — Workspace where the page will be created


#### `page_list_tree`

List the page hierarchy tree

- **Resource**: `Concept.Pages.Page`
- **Action**: `:list_tree` (read)


#### `page_recent_pages`

Get recently updated pages

- **Resource**: `Concept.Pages.Page`
- **Action**: `:recent_pages` (read)


#### `page_rename`

Rename the page title

- **Resource**: `Concept.Pages.Page`
- **Action**: `:rename` (update)


#### `page_reorder`

Change the page order among siblings

- **Resource**: `Concept.Pages.Page`
- **Action**: `:reorder` (update)


#### `page_reparent`

Move page to a new parent

- **Resource**: `Concept.Pages.Page`
- **Action**: `:reparent` (update)


#### `page_restore`

Restore an archived page

- **Resource**: `Concept.Pages.Page`
- **Action**: `:restore` (update)


#### `page_search_titles`

Search pages by title

- **Resource**: `Concept.Pages.Page`
- **Action**: `:search_titles` (read)

**Arguments:**

  - `query` (`String`, required) — Search term for page titles


#### `page_set_cover_color`

Set the page cover color

- **Resource**: `Concept.Pages.Page`
- **Action**: `:set_cover_color` (update)


#### `page_set_icon`

Change the page emoji icon

- **Resource**: `Concept.Pages.Page`
- **Action**: `:set_icon` (update)


---