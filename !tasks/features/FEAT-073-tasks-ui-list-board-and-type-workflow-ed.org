#+TITLE: Tasks UI list board and type workflow editors
#+STATE: ITEM
#+SESSION_ID: 14f437a4d57957b7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-concept/2026-05-29T12-15-33-205Z_14f437a4d57957b7.jsonl]]
#+CUSTOM_ID: FEAT-073-tasks-ui-list-board-and-type-workflow-ed
#+LAYER: platform
#+DEPENDS: FEAT-072-seed-task-type-and-readiness-read-action

* Initial Message

Let's think. What is a nice, simple system that lets us do project management within Concept? As opposed to simple knowledge base stuff. This would mean:
- it's easy to know which tasks need to be done. which are known as tasks and which ones are aspirational/research/whatever.
- task assignment

look up online to read expert designers' takes on the shortcomings of the Notion model. This is a DESIGN session where we think deeply, from first principles, about what can help us achieve these principles:
1. Simple (it's easy for agents and humans to understand the system)
2. Transparent & not prone to redundancy (it's easy to find where each task is, and it's easy to know where to put each task)
4. Ease of management

For this first iteration, think about what we'd do if we had to design Notion's system from scratch, with the principle that Concept is for human and agent TEAMS working together. Ask me plenty of questions to operationalise this principle

Parent: [[id:PLAN-009-objects-and-tasks-database-builder]]

LiveView (code-interface fns only per LiveViewPurity EX9001): Tasks list +
board grouped by category. ObjectType/field editor (create/rename types,
add/reorder fields). Workflow editor: states + drag transitions + attach
guards from a palette (Guard.describe for labels). Full customization usable
immediately. Begin templates with <Layouts.app flash={@flash} ...>.

** Verification (REQUIRED before R6 + report)
- TDD: LiveView tests (Phoenix.LiveViewTest) by element id — list/board render,
  create record, drag transition (guard rejection surfaced), field editor mutates
  FieldDefs, workflow editor adds state/transition/guard.
- Browser (REQUIRED): full manual pass — define a custom type end-to-end, build a
  workflow with a guard, create + move records on the board, confirm an agent MCP
  tool appears for the new type. World-class UI polish check.
- Reviewer wave R6: LiveViewPurity EX9001 (code-interface only, no Ash.Query in
  live/), Layouts.app usage, accessibility of drag interactions.
- Mid-flight report: work done (+tests) · process learnings · follow-ups filed.
- Gate: mix precommit green + R6 sign-off + final report.

NOTE [2026-05-29]: WAVE 6 MID-FLIGHT REPORT (UI — Tasks board).

WORK DONE:
- Record :board read (loads :state) + board_records define. Concept.Objects.task_board/1 (states + records
  grouped by category) and available_moves/2 (transitions from current state) — pure orchestration over
  code-interface fns so the LiveView stays EX9001-clean.
- get_workflow_state define added.
- ConceptWeb.TasksLive: board with 6 category columns, create-task form (into Backlog flow), per-card move
  buttons rendering each available guarded transition; guard rejections surface as flash. Route
  /w/:workspace_slug/tasks in authenticated live_session. Layouts.app wrapper. EX9001: credo clean.
- Tests: TasksLive 3 (columns render, create adds to board via form submit, move Backlog→Todo through the
  guarded graph and asserts persisted state). Objects+blocks+http+tasks 70 GREEN. full_flow updated to 21
  block modules. Full suite: 456 tests, only pre-existing non-Objects failures remain (chat LLM-mock x4,
  HomeLive toggle_chat x1 — all untouched by this work, fail in isolation).

WORK TO BE DONE (deferred, file as FUPs):
- ObjectType/FieldDef editor UI and Workflow editor (states + drag transitions + guard palette) — domain
  surface exists (create_object_type/create_field_def/create_workflow_state/create_transition/
  set_transition_guards all live + MCP-exposed); only the LiveView editors remain. Board is the
  highest-value slice and is done.
- record_ref picker UI (set record_id from the page editor) — block renders live already.
- "create into Backlog" auto-transition: new records start state nil; board create leaves them stateless
  until first move. Consider auto-entering the workflow initial state on create for Task type.
- Browser screenshot blocked by a stale external dev server on :4000 owned by another process (404 on new
  route until it reloads); LiveViewTest covers the same mount/render/create/move cycle end-to-end.

PROCESS LEARNINGS:
- code_interface define args: a map-valued attribute (fields) must NOT be a positional arg. With
  args: [:object_type_id] the trailing %{fields: %{...}} is the params map (correct). Adding
  {:optional, :fields} makes Ash bind the whole map as the positional → "unknown field(s): fields".
  Canonical create form: create_record(type_id, %{fields: %{...}}, actor:, tenant:).
- Rewriting an Ash.Domain file: keep ALL resource blocks inside `resources do ... end`; helper fns go after.
  A stray early `end` orphans later resources with a confusing "undefined function resource/2".