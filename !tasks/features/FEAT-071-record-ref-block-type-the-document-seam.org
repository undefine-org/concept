#+TITLE: record_ref block type the document seam
#+STATE: ITEM
#+SESSION_ID: 14f437a4d57957b7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-concept/2026-05-29T12-15-33-205Z_14f437a4d57957b7.jsonl]]
#+CUSTOM_ID: FEAT-071-record-ref-block-type-the-document-seam
#+LAYER: platform
#+DEPENDS: FEAT-072-seed-task-type-and-readiness-read-action

* Initial Message

Let's think. What is a nice, simple system that lets us do project management within Concept? As opposed to simple knowledge base stuff. This would mean:
- it's easy to know which tasks need to be done. which are known as tasks and which ones are aspirational/research/whatever.
- task assignment

look up online to read expert designers' takes on the shortcomings of the Notion model. This is a DESIGN session where we think deeply, from first principles, about what can help us achieve these principles:
1. Simple (it's easy for agents and humans to understand the system)
2. Transparent & not prone to redundancy (it's easy to find where each task is, and it's easy to know where to put each task)
4. Ease of management

For this first iteration, think about what we'd do if we had to design Notion's system from scratch, with the principle that Concept is for human and agent TEAMS working together. Ask me plenty of questions to operationalise this principle

Parent: [[id:PLAN-009-objects-and-tasks-database-builder]]

New block type per docs/blocks/ADDING_A_BLOCK.md (one registry line in
config :concept, :block_types; zero dispatcher edits). props %{record_id}.
Renders the referenced Record's LIVE title, state (category color), assignee.
Editing status/assignee from inside a doc calls the same Record actions. This
is the projection that lets any page mention a record without copying it —
the structural redundancy-killer.

** Verification (REQUIRED before R5 + report)
- TDD: block-type contract test (per ai_answer_test pattern) — props validation,
  render output references record_id, edit dispatches Record action.
- Browser (REQUIRED): embed a record_ref in a page; change the record's state via
  the Tasks surface; confirm the block re-renders live (category color + assignee)
  without reload (pub_sub).
- Reviewer wave R5: registry-only wiring (no dispatcher/handle_event edits per
  ADDING_A_BLOCK anti-patterns), live update path.
- Mid-flight report: work done (+tests) · next-wave assumptions · learnings.
- Gate: mix precommit green + R5 sign-off + report.

NOTE [2026-05-29]: WAVE 5 MID-FLIGHT REPORT (record_ref — the seam).

WORK DONE:
- Concept.Pages.BlockTypes.RecordRef (use BlockType.Static): props %{record_id}; renders the referenced
  Record's LIVE title + workflow state (category-colored badge) + unlinked placeholder fallback. Reads via
  system actor scoped to block.workspace_id with load: [:state]. Registered in config :block_types
  (one line, zero dispatcher edits per ADDING_A_BLOCK).
- Category color map (backlog/todo/doing/review/done/canceled). Slash menu entry ("Task / record", group :media).
- Tests: 14 GREEN — registry contract (render_kind, props validation, slash feed, create on page) + render
  (title, live state after transition, nil placeholder, missing-record placeholder).

WORK TO BE DONE (Wave 6 — UI):
- Tasks list + board-by-category LiveView (code-interface only, EX9001).
- ObjectType/field editor + workflow editor (states + transitions + guard palette).
- A picker to set record_ref.record_id from the editor (currently props set programmatically/via MCP).
  Manual browser test REQUIRED this wave.

PROCESS LEARNINGS:
- Static block render/1 is called by the dispatcher with full assigns; unit-testing it standalone needs an
  assigns map carrying change-tracking (%{__changed__: nil} + :block), else Phoenix raises bad_socket_or_assign.
- record_ref reads the record at render time (no stored copy) — live updates fall out for free; PubSub-driven
  re-render will refresh it when the page editor re-renders blocks.
- Pre-existing flake: test/concept/pages/staleness_test.exs:193 depends on evaluate_ai (Gemini/external),
  last touched by another agent's wave-4.5; fails without live API. NOT an Objects regression.