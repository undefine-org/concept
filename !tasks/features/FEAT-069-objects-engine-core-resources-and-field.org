#+TITLE: Objects engine core resources and field types
#+STATE: ITEM
#+SESSION_ID: 14f437a4d57957b7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-concept/2026-05-29T12-15-33-205Z_14f437a4d57957b7.jsonl]]
#+CUSTOM_ID: FEAT-069-objects-engine-core-resources-and-field
#+LAYER: platform

* Initial Message

Let's think. What is a nice, simple system that lets us do project management within Concept? As opposed to simple knowledge base stuff. This would mean:
- it's easy to know which tasks need to be done. which are known as tasks and which ones are aspirational/research/whatever.
- task assignment

look up online to read expert designers' takes on the shortcomings of the Notion model. This is a DESIGN session where we think deeply, from first principles, about what can help us achieve these principles:
1. Simple (it's easy for agents and humans to understand the system)
2. Transparent & not prone to redundancy (it's easy to find where each task is, and it's easy to know where to put each task)
4. Ease of management

For this first iteration, think about what we'd do if we had to design Notion's system from scratch, with the principle that Concept is for human and agent TEAMS working together. Ask me plenty of questions to operationalise this principle

Parent: [[id:PLAN-009-objects-and-tasks-database-builder]]

ObjectType, FieldDef, Record, RecordLink resources (use WorkspaceTenanted +
AshArchival + AshPaperTrail). FieldType behaviour + registry (config
:concept, :field_types) + built-in field types: text, number, select, date,
user, relation, checklist, url. Record.Changes.ValidateFieldsForType
(generalize Block.Changes.ValidatePropsForType). Title denormalized from a
designated display FieldDef. JSONB+GIN indexes for field filter/sort.

** Verification (REQUIRED before R1 + report)
- TDD: FieldType.validate/2 per built-in type (red→green); Record :create with
  ValidateFieldsForType (valid + invalid field-bag); RecordLink relation edge.
- Browser: n/a this wave (no UI yet) — defer to a seeded iex demo proving create/read.
- Reviewer wave R1: policy/tenancy on all 4 resources, JSONB index presence,
  registry parity with block_types idiom.
- Mid-flight report: work done (+tests) · next-wave assumptions · learnings.
- Gate: mix precommit green + R1 sign-off + report.

NOTE [2026-05-29]: WAVE 1 MID-FLIGHT REPORT (engine core).

WORK DONE (commits e4c23a4 + ancestors):
- FieldType behaviour + FieldTypes registry + FieldTypeAttr (block_type pattern, lifted).
- 8 built-in field types: text, number, select, date, user, url, checklist, relation.
- Resources: ObjectType, FieldDef, Record, RecordLink (WorkspaceTenanted + archival).
- Changes: SetWorkspaceFromTenant (tenant-derived workspace_id, dropped redundant arg),
  SlugifyKey, AssignDefaults (defaults + title derivation), ValidateFieldsForType
  (generalized ValidatePropsForType: required + type + unknown-key + relation skip),
  per-resource AssignAfterLastSibling, RunTransition (Wave 1 stub).
- Migration 20260529133156 (single-col GIN on fields); snapshots; domain registered.
- Tests: 26 (field types unit + engine integration) GREEN. No drift. No regressions (33 w/ block contract).

WORK TO BE DONE (next: Wave 2):
- Workflow/WorkflowState(category invariant)/Transition resources.
- Guard behaviour + registry + 4 built-ins; full RunTransition (graph + guards + error agg).
- Then Wave 3 wires ObjectType.workflow_id -> Workflow relationship + readiness by category.

PROCESS LEARNINGS:
- Concurrent agents run `git clean`/revert on the worktree: untracked files vanished mid-wave
  and config/config.exs got reverted twice. MITIGATION: commit immediately after each cohesive
  unit, not at wave end. Restoring own committed paths via `git checkout HEAD -- <mypaths>` is safe.
- Composite GIN [workspace_id, fields] fails (uuid has no gin opclass). FIX: index [:fields],
  using: "gin", all_tenants?: true to avoid tenant-attr prepend.
- Ash code_interface: a trailing map arg is treated as params only if the action arg is passed
  via params map (define args list maps positionals). For map-valued attrs (fields), define with
  args: [:object_type_id] and pass %{fields: ...} as the params map — cleaner than optional positional.
- A duplicate stale migration (132930 + 133156) caused "relation records already exists"; the first
  deleted file resurfaced via FS flakiness. Verify `ls priv/repo/migrations | grep` after deletes.