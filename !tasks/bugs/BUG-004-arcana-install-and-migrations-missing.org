#+TITLE: Arcana install and migrations missing
#+STATE: DONE
#+SESSION_ID: 14e3b087d426cfb5
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-concept/2026-05-16T16-09-32-240Z_14e3b087d426cfb5.jsonl]]
#+CUSTOM_ID: BUG-004-arcana-install-and-migrations-missing
#+LAYER: ingestion

* Initial Message

Do a critical review of Arcana's integration into Concept and how you'd make the frontend interesting to use w.r.t those features. Go wild, go free, be thorough

* Intention
~mix arcana.install~ was never executed: ~priv/repo/migrations/~ contains only the pgvector enable, no ~arcana_collections~ / ~arcana_chunks~ / ~arcana_documents~ / ~arcana_entities~ / ~arcana_relationships~ tables. Every Arcana DB call fails at runtime.

* Files
- ~mix.exs~ (aliases — add ~arcana.install~ to ~setup~)
- ~priv/repo/migrations/*~ (new migration files generated by ~mix arcana.install~ + ~mix arcana.graph.install~)
- ~config/test.exs~ (Mox embedder for tests)

* Implementation
1. Run ~mix arcana.install~ (Igniter) — applies repo config + base RAG migrations.
2. Run ~mix arcana.graph.install~ — adds graph tables.
3. Add a ~mock_embedder.ex~ in ~test/support/~ implementing ~Arcana.Embedder~ with deterministic 768-vec from text hash; register in ~config/test.exs~ as ~config :arcana, embedder: {:custom, module: Concept.Knowledge.MockEmbedder}~.
4. Update ~mix setup~ alias chain so a fresh checkout works.

* Acceptance
- ~mix ecto.reset~ creates all Arcana tables.
- ~mix test~ runs with Mox embedder (no Gemini calls in unit tests).
- ~Arcana.Collection.get_or_create("workspace:abc", Concept.Repo, "test")~ succeeds in IEx.

NOTE [2026-05-16]: Completed during Wave -1 foundation: mix arcana.install + mix arcana.graph.install + mix ash_ai.gen.chat ran; migrations relocated from priv/concept_repo to priv/repo (installer mis-detected repo name); MockEmbedder + Gemini-2.5-flash config wired.